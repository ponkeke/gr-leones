// Configuración del Simulador de Costos.
//
// PROVISIONAL: estas opciones de cuotas son solo para que el selector funcione mientras la
// empresa no entregue sus plazos reales. Para cambiarlas, edita esta lista (números enteros > 0);
// el selector y la validación se actualizan solos. Cuando existan en la base de datos, esta
// lista pasará a venir de la API (ver `src/services/api.js`).
export const OPCIONES_CUOTAS = [6, 12, 24, 36, 48, 60]
